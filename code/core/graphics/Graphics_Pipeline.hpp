#pragma once

namespace RoseGold::Core::Graphics
{
	class Shader
	{
	public:
		enum class Type
		{
			Vertex,
			Pixel
		};
	};

	class PipelineState
	{
		// (IA) Input assembler

		// (VS) Vertex shader

		// (HS) Hull shader

		// (TS) Tesselation

		// (DS) Domain shader

		// (GS) Geometry shader

		// (SO) Stream output

		// (RS) Rasterizer

		// (PS) Pixel shader

		// (OM) Output merger
	};
}